# Примеры команд

```bash
python -m calculus_tool --help
python -m calculus_tool demo
python -m calculus_tool limit --expr "sin(x)/x" --at 0
python -m calculus_tool derivative --expr "x**3" --order 2
python -m calculus_tool integral --expr "x" --a 0 --b 1